Click the toolbar button to highlight the exportable elements, then right click into a exportable area to open the context menu. The context menu will contain the submenu labled "Table to CSV" containing entries for HTML or Text export.
