Import urls from the clipboard or any text file as bookmarks or open them as tabs 
Click the toolbar button to open the importer tab view, then provide the text from which to extract the URLs
Tabs can optionally be opened into a Firefox tab group — either a new group (with an optional name and color) or an existing group already open in the window. 
